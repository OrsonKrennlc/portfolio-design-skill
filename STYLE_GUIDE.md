# 项目风格说明与 Web 样例索引

更新日期：2026-09-16。范围：首页、建筑作品、小工具集及风格样例页。

> 以建筑作品册为基础的编辑式个人网站：黑白组织章节，深绿建立识别，宋体与等宽字体分担人文叙事和理性信息，影像与留白构成阅读节奏。

## 1. 使用方式

- [打开样例网页](../style-guide.html)：集中查看视觉基础、真实组件、交互和响应式表现；支持 `?lang=zh` 与 `?lang=en`。
- [设计规范](DESIGN_GUIDELINES.md)是设计决策的权威来源。本说明负责解释与索引，不建立另一套全站规范。
- 本文区分**现有样式**与**样例补充**。表单和状态练习属于后续扩展参考，并不表示作品集已经包含相应业务。
- 样例复用三份现有 CSS，新增规则限定在 `.style-guide` 页面。没有新增框架、构建流程或共享导航注入。

## 2. 风格定位

| 特征 | 可见证据 | 延续方式 |
| --- | --- | --- |
| 建筑作品册感 | 大标题、编号、参数、细线与整幅图像 | 先确定阅读顺序，再安排图文比例 |
| 影像主导 | 山景、建筑渲染与图纸、方形分类封面 | 用真实素材表达内容，避免装饰压过作品 |
| 安静与留白 | 大页边距、长段落间距、低密度画布 | 用空白分组，不把每段都套成卡片 |
| 黑白章节 | 山景进入黑底介绍，再切换至白底履历 | 明暗变化表达章节，不随机换主题 |
| 深绿识别 | 签名、章节标题、导航、地图点与主按钮 | 界面强调色保持 `#02542D`，影像保留原色 |
| 个人表达 | 宋体、等宽数字、手写签名与藏文 | 保留字体角色边界，个人语气集中在介绍和页脚 |

**首页**是个人叙事：山景与错位叠字建立开场，暗色介绍与引语放慢阅读，白底教育和奖项补充证据，方形作品入口引导继续浏览。奖项围绕作品组织，归并展示不同赛区的结果。

**建筑页**是作品档案：概览和浅色地图并置，快捷索引提供直接入口，项目按编号及交替图文展开。详情以完整大图、元数据和长描述提供深读层级。

**小工具集**是编辑式目录：大标题和短引言之后，以抽象图形预览、名称、用途、标签及操作组成条目。主操作打开应用，次操作查看源码。

## 3. 视觉基础

### 3.1 色彩

| 角色 | 色值 | 使用边界 |
| --- | --- | --- |
| 品牌深绿 | `#02542D` | 标题、签名、激活状态、关键控件 |
| 白色画布 | `#FFFFFF` | 主体页面与内容层 |
| 黑色 | `#000000` | 主文字与首页暗色章节 |
| 次级文字 | `#676767` | 白底说明、元数据标签 |
| 弱化灰 | `#808080` | 现有辅助色；白底普通小字达不到 4.5:1，不作通用正文色 |
| 分隔线 | `#EAEAEA` | 装饰分组；不单独承担输入框边界或焦点识别 |
| 暗底次级文字 | `#D0D0D0` | 黑底介绍与引语说明 |

样例的小字采用满足普通文本对比要求的颜色。交互边界使用深绿或较深灰，浅灰线只用于装饰。表单错误同时出现文字与边框，不只靠颜色提示。

### 3.2 字体与层级

| 字体 | 角色 | 样例 |
| --- | --- | --- |
| Noto Serif SC | 中文标题与正文 | 建筑项目、于方寸之间，见万千气象。 |
| Anonymous Pro | 英文、编号、日期和技术标注 | ARCHITECTURE / 01 / 2026 |
| Caveat | 品牌签名 | Junyang Shen |
| Noto Serif Tibetan | 藏文原文 | 首页藏文名言 |

常见章节标题为桌面 `48px`、手机 `32px`；建筑项目标题桌面 `30–36px`、手机 `26px`。工具集标题为独立展示层级，桌面使用 `clamp(48px, 7vw, 88px)`。正文通常 `16–20px`，长描述行高约 `1.8–2`；编号与辅助信息约 `12–14px`。

这些数值对应不同组件，不要求所有标题使用同一字号。英文允许自然换行，文本容器设 `min-width: 0`，避免长词撑开布局。

### 3.3 布局与尺度

- 标准内容最大宽度 `1200px`；长描述约 `720px`。
- 常见页边距：桌面 `150px`、中屏 `50px`、手机 `20px`；Hero 另有流动边距。
- 主要断点 `1200px`、`768px`；建筑概览和工具目录还使用 `900px` 单列切换。
- 桌面导航高 `100px`，移动导航高 `72px`；手机菜单展开后显示栏目及语言控件。
- 主段落留白常见 `80–150px`；组件内部约 `20–40px`。样例沿用同一尺度并适度收紧，便于比较。
- 项目封面 `8:5`，首页分类入口 `1:1`，详情图用 `contain` 保留全图。

### 3.4 表面与几何

主体采用开放式分组与细线。局部圆角职责明确：图片 `4px`、桌面地图和弹窗 `16px`、地图提示 `8px`、移动地图 `12px`；工具标签为胶囊，肖像与地图点为圆形。移动建筑详情为无圆角全屏层。

默认项目条目无阴影，悬停或聚焦使用 `0 15px 40px rgba(0,0,0,.10)`；弹窗使用 `0 20px 50px rgba(0,0,0,.20)`。不能将网站概括为完全无圆角或无阴影。

## 4. 图片、图形和文案

自然山景、建筑材料、光线与空间是主要影像语言。低饱和是整体倾向，不应通过统一滤镜改变作品原色。列表封面允许裁切，详情与图纸保留完整信息。

山景中的黑色渐变承担摄影到暗色正文的过渡；手机藏文背后的羽化暗部服务文字可读性。工具预览使用网格、圆环、点阵与大字母，属于现有抽象封面，不是产品界面截图。

文案优先说明对象、地点、类型、设计内容和操作。个人段落允许含蓄的短句，项目段落以事实为中心。按钮使用“打开应用”“查看源码”等明确动词。中英文各自构成完整界面，品牌、拉丁文、藏文与专名可保留原文；标题、说明、元信息、替代文本和可访问名称均应有两种语言。

## 5. 样例覆盖表

“所有样式”指当前网站的视觉基础和全部主要组件类型，不逐一陈列每个 CSS 选择器。同一组件的页面微调以源样式为准。

| 区域 | 覆盖内容 | 来源 / 性质 |
| --- | --- | --- |
| 导航、页脚 | 签名、当前项、语言切换、移动菜单与署名 | 全局组件 |
| 01 色彩 | 语义色板、文字与底色配对 | 全局 token |
| 02 字体 | 中文、英文、签名、藏文、展示字、正文与元信息 | 全局字体与组件层级 |
| 03 布局 | 容器、间距、细线、圆角、阴影 | 全站尺度 |
| 04 影像 | 山景、五层叠字、暗色介绍、引语、方形分类 | 首页组件的缩放样例 |
| 05 内容 | 建筑条目、交替图文、元数据、履历奖项、两种工具预览 | 复用现有组件类 |
| 06 控件 | 主次按钮、标签、各交互状态、表单与反馈 | 表单为补充 |
| 07 交互 | 快捷索引、地图标记和预览示意、双图画廊弹窗 | 完整地理浏览链接至建筑页 |
| 08 Web 要求 | 语义、键盘、对比、触控、重排与减少动态效果 | 样例验收依据 |

## 6. Web 实现要求

以语义化 HTML 和 WCAG 2.2 AA 的相关要求作为样例目标，不等同于对全站作无障碍认证。

1. **语义**：单个 `h1`，章节顺序清楚；使用页面地标，数据用 `dl` 或真实表格；链接导航、按钮执行动作。提供跳过导航入口。
2. **对比**：普通文本至少 `4.5:1`；大字至少 `3:1`（约 `24px` 常规或 `18.7px` 粗体）。有识别作用的控件边界与状态至少 `3:1`。
3. **键盘**：Tab、Enter/Space 可完成操作；焦点清楚且不被顶栏遮挡；弹窗支持 Esc，关闭后焦点返回触发器。
4. **触控**：样例主要操作目标至少 `44×44px`。WCAG 2.2 AA 的目标尺寸基线是 `24×24px`，有间距等例外，二者不可混称。
5. **重排**：`320 CSS px` 宽度仍能阅读与操作，不靠全局裁切隐藏溢出。英文和网格均需换行；宽表格仅在自身容器必要时滚动。
6. **动效**：采用约 `150/300/500ms` 的反馈尺度；尊重 `prefers-reduced-motion`。原首页有 `1.8s` 循环箭头，样例改为一次演示。
7. **图片**：设定尺寸或比例，非首屏延迟加载；内容图有准确双语 `alt`，装饰图空 `alt`；字体有系统回退。
8. **表单**：可见标签、必填说明、关联字段的错误文字与状态反馈。样例只本地校验，不发送或保存信息。
9. **语言**：复用 `js/i18n.js`，更新页面 `lang`；固定英文、拉丁文、藏文标注对应语言，不另建语言状态。
10. **工程**：无新增框架或打包依赖，使用相对路径；导航保留在 HTML；建筑生成数据不手工修改。

参考：[W3C WCAG 2.2 快速参考](https://www.w3.org/WAI/WCAG22/quickref/)、[MDN 原生 dialog](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dialog)。

## 7. 现状与建议的边界

- 旧规范的部分建议已经落地，例如简历最大宽度为 `1200px`。本说明以当前代码为现状依据。
- 原站包含 `#808080` 小字、28px 画廊圆点点击区和循环箭头。样例改善相关可读性与操作尺度，仅作用于样例页。
- 输入框、选择框、复选框和验证反馈是遵循现有风格的补充示例，业务接入仍需确定数据和提交逻辑。
- 地图示意明确标注为样式演示，不代表真实坐标；真实地图继续由建筑页提供。
- 本次不改变原有页面视觉，也不自动把补充规则扩散到全站。

## 8. 文件与验收

| 文件 | 职责 |
| --- | --- |
| `style-guide.html` | 完整中英文样例 |
| `css/style-guide.css` | 页面限定的排版、补充组件和可访问性校准 |
| `js/style-guide.js` | 弹窗、画廊和本地表单反馈 |
| `js/i18n.js` | `guide.*` 双语文案；语言按钮选择器限定为按钮，避免根元素出现无效 ARIA |
| `docs/DESIGN_GUIDELINES.md` | 样例及补充规则的决策边界 |

交付检查包括数据同步、页面资源、JavaScript 语法与差异空白。浏览器检查覆盖两种语言、桌面与窄屏、菜单、弹窗与焦点返回、画廊及表单反馈，结果在交付时注明。


### 2026-09-16 验收记录

- 数据与工程：建筑数据同步、站点 HTML/资源/CSS/i18n 校验通过；修改的 JavaScript 语法检查及 `git diff --check` 通过。
- 浏览器：使用系统 Edge 无头模式；中英文在 `320 / 390 / 768 / 900 / 1024 / 1440px` 下无横向溢出，英文额外检查 `769px` 临界宽度。
- 交互：移动菜单、语言切换、动态反馈翻译、表单必填与成功状态、弹窗开关、方向键翻图、Esc 关闭、焦点返回、减少动态效果均通过。禁用 JavaScript 时仍可阅读静态内容和访问移动导航。
- 自动可访问性：axe-core 4.10.3，运行 WCAG A/AA（含 2.1/2.2 AA）规则；桌面中文、移动英文、769px 英文及打开的弹窗均为 0 条违规。自动检查不覆盖全部无障碍要求。
- 视觉：已查看桌面首屏、手机英文首屏、控件、影像叙事和手机画廊截图；图片裁切、文字层级及焦点样式符合样例用途。
- 回归：三个原入口页面的中英文切换通过。修正根元素被误标 `aria-pressed` 的问题，页面视觉未改动。
