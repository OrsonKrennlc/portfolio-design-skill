---
name: portfolio-design-skill
description: 按建筑作品册式编辑风格设计或改造网页：黑白章节、深绿强调、中文宋体与英文等宽字体、真实影像和大留白。适用于用户希望延续这套作品集风格的个人网站、项目档案、摄影页面、工具目录及精简样式手册；不用于无风格关联的一般前端任务。
---

# 建筑作品册式网页设计

把网站当作可浏览的作品册：影像建立叙事，字体、编号、参数与细线建立秩序。复用设计语言，根据新内容重新编排，不复制原站的个人身份、作品、履历或整段页面。

## 开始设计

1. 读取目标项目的规则、已有页面、样式变量、语言机制和素材。确认当前任务是新页面、局部改造，还是样式手册；只处理用户要求的范围。
2. 先用简短大纲说明阅读顺序、主图作用与关键操作，然后继续实现；无需为常规排版选择另设批准步骤。
3. 根据内容选择叙事页、作品档案或编辑目录，不要求每个页面包含 Hero、地图、履历、引语和画廊。
4. 查看 [设计规则](references/design-language.md) 中对应的基础与组件段落。新建页面可从 [CSS 变量种子](assets/tokens.css) 提取变量；已有项目优先映射现有 token，不直接覆盖全站样式。

## 风格锚点

- **黑白为主体，深绿作识别。** 默认白底黑字；黑底章节用于叙事转场，深绿 `#02542D` 用于标题、导航状态与关键操作，不铺满所有模块。
- **字体各司其职。** 中文使用 Noto Serif SC，英文与数字使用 Anonymous Pro；Caveat 只用于确有需要的品牌签名；Noto Serif Tibetan 只用于真实藏文。不要为凑齐字体添加藏文、引语或签名。
- **影像先于装饰。** 用真实项目、自然、建筑和旅行素材表达内容。列表封面可裁切，图纸与详情图保留全图；不统一加滤镜改变作品原色。
- **开放排版。** 以宽幅图像、留白、编号、对齐和细线组织内容，避免把每段文字包成圆角卡片、仪表盘或营销功能宫格。
- **局部圆角与阴影有明确用途。** 图片 4px、桌面地图和弹窗 16px；工具标签可用胶囊。普通项目条目常态无阴影，悬停、聚焦或弹窗才出现适度层级。不要写成全站绝对禁用圆角、阴影或渐变。
- **动效安静。** 反馈采用约 150 / 300 / 500ms；图片缩放最多约 1.05。避免循环漂浮、彩色故障效果、强视差和大面积发光。尊重减少动态效果偏好。

## 按内容组织页面

- **叙事页：** 一张有意义的主图与一个核心句开场，必要时进入暗色介绍，再以浅色事实章节和作品入口收束。五层错位叠字仅在适合的摄影序章使用，不强加给所有标题。
- **作品档案：** 概览与快捷索引 → 编号项目列表 → 详情。桌面可交替图文，移动端恢复自然阅读顺序。地图只有在真实地理信息有用时才加入，并提供列表替代入口。
- **工具目录：** 简短引言 → 开放式条目。条目按图形预览、名称、用途、少量标签与明确操作组织；抽象图形不能冒充真实应用截图。
- **样式手册：** 展示色彩、字体、布局、影像、内容组件、控件和交互规则。每类默认只保留一个必要样例，旁注比例、结构和状态；仅在需要比较时增加变体。不要再次展示整份作品集。

页面中的姓名、年份、地点、奖项和项目参数来自当前用户资料。缺少素材时明确说明，并使用清楚标记的样例或真实空状态；不要沿用来源网站的人物经历或制造项目事实。

## 实现边界

- 沿用目标项目技术栈。新建简单静态页面时可用 HTML/CSS/JavaScript；已有 React、Vue 或其他框架时，在其体系内实现同一设计语言，不为风格迁移重建工程。
- 中英文采用一次只显示当前语言的完整界面，不默认把每个标题写成中英并排。需要双语时复用现有语言状态，覆盖标题、正文、元数据、按钮、图片替代文本、可访问名称和动态反馈；更新页面 `lang`，为固定外文标注相应语言。
- 正文默认 16px 起、行高约 1.8；字号、容器和间距按组件角色变化。标准内容宽度约 1200px、长文约 720px、手机页边距 20px。具体尺度见参考文件，不用固定高度或隐藏溢出掩盖换行问题。
- 普通小字使用 `#676767`，不要沿用白底 `#808080` 的对比度缺陷；`#EAEAEA` 只作装饰分隔，不单独承担输入框边界或焦点识别。暗底焦点使用可辨认的浅色。
- 链接负责导航，按钮负责动作；整块入口仍需语义和键盘支持，避免嵌套交互元素。菜单、弹窗和画廊实现实际可用的状态与焦点行为。
- 表单、地图示意等扩展组件要说明性质。未接入的表单不能声称已发送；演示地图不能伪装成真实定位。风格任务本身不隐含新增后端、地图服务或发布操作。

## 交付前检查

按实际改动检查，不为未使用的组件引入测试或依赖：

- 查看桌面和窄屏截图，确认影像、标题、正文与留白的层级，检查是否退化成重复卡片、装饰堆叠或原站内容复制。
- 布局检查 320px 窄屏、常用手机宽度和桌面；有双语时检查英文长词及断点两侧。正文可重排，不依赖全局 `overflow-x: hidden`。
- 验证真实入口、菜单、语言切换，以及新增弹窗的 Esc、焦点圈定和焦点返回；画廊保留按钮操作，表单反馈关联字段并可被读出。
- 主要操作目标以 44×44px 为设计目标；检查文字与控件对比、图片替代文本、焦点可见性及减少动态效果。自动无障碍检测只能作为证据之一，不宣称完整认证。
- 运行项目已有的相关资源、语法或构建检查。交付时说明做了什么、验证了什么和未能验证的限制，不复用来源对话的历史测试结论。

本 skill 的规则已从用户提供的风格文档和对话中提炼，运行时不需要访问分享链接或原网站。来源冲突与适用边界见参考文件末尾。
